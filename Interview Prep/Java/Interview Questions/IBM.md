1. What are the issues that would be coming while migrating the java version
2. Write a employee class with default constructor and parameterized constructor, call the parameterized constructor inside the default constructor without using new
3. Initialize a new variable and assign a value to it, and pass the variable directly to the parameter, y it is throwing error
4. Explain the types of Garbage collectors
5. What would happen if we use garbage collectors and use case of each garbage collectors and when to use which
6. What are the types of thread pools
7. Write Fibonacci series such that there would be 2 threads and one thread needs to print first value and the second needs to print the next value, maintain the order
8. If we have 10 tables how many repositories are required
9. There is a table Employees, with id, name and managerId as the columns, except CEO all the other employee would have manager id, print the name in the hierarchy of the organization chart
10. Explain what is a spring container
11. What happens when bean A is waiting for bean B (a circular dependency), how would u resolve it