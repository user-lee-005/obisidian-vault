## Round 1
1. Insert and merge intervals:
	Given:
	```java
	public List<List<Integer>> insertAndMergeIntervals(int[][] arr, int[] newInterval) {
		// int[][] arr = [[1,3], [2, 4], [5, 10], [7, 12]]
		// int[] newInterval = [8, 14]
		// TODO merge all the intervals and insert the new interval in correct place and merge that also if requred
	}
	```
2. Validate Binary Search Tree:
	Given:
	```java
	public boolean isValidBST(Node root) {
		// TODO validate this tree that if it is a binary search tree or not
	}
	```
3. Find the minimum in rotated sorted array:
	```java
	public int minimumInSortedArray(int[] nums) {
		// int[] nums = [5, 4, 1, 2, 3]
		// TODO find the minimum element in this array
		// Follow up there can be an edge where the array might be sorted in descending order
	}
	```
